services:
  level2-test:
    build:
      context: .
      dockerfile: Dockerfile.test
    image: llm-veil-level2-test:local
    environment:
      CARGO_TERM_COLOR: always
      RUST_BACKTRACE: "1"
    volumes:
      - level2-target:/workspace/target

volumes:
  level2-target:
